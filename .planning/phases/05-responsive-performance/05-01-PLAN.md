---
phase: 05-responsive-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tailwind.config.js
  - app/globals.css
  - app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Colors render as warm dark (Sumi) not stark black"
    - "Text renders as cream (Washi) not pure white"
    - "Accents render as burgundy (Beni) not firebrick"
    - "Gold highlights (Kincha) available for premium accents"
  artifacts:
    - path: "tailwind.config.js"
      provides: "Japanese-inspired color palette"
      contains: "sumi.*1a1614"
    - path: "app/globals.css"
      provides: "CSS custom properties for colors"
      contains: "--color-sumi"
    - path: "app/layout.tsx"
      provides: "Font imports for Inter UI"
      contains: "Inter"
  key_links:
    - from: "tailwind.config.js"
      to: "app/globals.css"
      via: "color tokens match CSS variables"
      pattern: "sumi|washi|beni|kincha|hai|kitsune"
---

<objective>
Migrate the design system to Japanese-inspired color palette and typography per DESIGN-SYSTEM.md.

Purpose: Establish the sophisticated, premium aesthetic foundation before content rebuild. All subsequent plans use these colors.
Output: Updated Tailwind config with new color tokens, CSS custom properties, and Inter font added for UI elements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN-SYSTEM.md

# Current implementation
@tailwind.config.js
@app/globals.css
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tailwind color palette with Japanese-inspired tokens</name>
  <files>tailwind.config.js</files>
  <action>
    Add new color tokens to tailwind.config.js while KEEPING existing brand-* colors for gradual migration:

    ```js
    colors: {
      // Existing (keep during migration)
      'brand-black': '#120c0d',
      'brand-white': '#f5f5f5',
      'brand-red': '#b22222',

      // NEW: Japanese-inspired palette from DESIGN-SYSTEM.md
      'sumi': '#1a1614',        // Ink black - primary background
      'washi': '#faf8f5',       // Paper white - primary text
      'beni': '#8b2635',        // Burgundy - accent
      'kincha': '#c4a35a',      // Gold tea - highlights
      'hai': '#6b6560',         // Ash gray - secondary text
      'kitsune': '#d4c4a8',     // Fox tan - warm backgrounds

      // Gradient colors for Night/Deep/Warm backgrounds
      'sumi-light': '#2d2825',  // Night gradient end
      'sumi-deep': '#1f1a18',   // Deep gradient end
      'sumi-warm': '#3d3530',   // Warm gradient end
    }
    ```

    Also add the content path for lib/ to ensure colors used in content files are not purged:
    ```js
    content: [
      './pages/**/*.{js,ts,jsx,tsx,mdx}',
      './components/**/*.{js,ts,jsx,tsx,mdx}',
      './app/**/*.{js,ts,jsx,tsx,mdx}',
      './lib/**/*.{js,ts,jsx,tsx,mdx}',  // ADD THIS
    ],
    ```
  </action>
  <verify>
    Run: `npx tailwindcss --help` (confirms Tailwind installed)
    Check: grep -E "sumi|washi|beni|kincha|hai|kitsune" tailwind.config.js (all 6 should appear)
  </verify>
  <done>
    tailwind.config.js contains all 6 Japanese color tokens plus gradient variants, existing brand-* colors preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CSS custom properties and Inter font</name>
  <files>app/globals.css, app/layout.tsx</files>
  <action>
    In app/globals.css, add CSS custom properties in :root for design tokens:

    ```css
    :root {
      /* Colors from DESIGN-SYSTEM.md */
      --color-sumi: #1a1614;
      --color-washi: #faf8f5;
      --color-beni: #8b2635;
      --color-kincha: #c4a35a;
      --color-hai: #6b6560;
      --color-kitsune: #d4c4a8;

      /* Typography */
      --font-heading: 'Cormorant Garamond', serif;
      --font-body: 'EB Garamond', serif;
      --font-ui: 'Inter', sans-serif;

      /* Spacing (8px base) */
      --space-1: 0.25rem;
      --space-2: 0.5rem;
      --space-3: 0.75rem;
      --space-4: 1rem;
      --space-6: 1.5rem;
      --space-8: 2rem;
      --space-12: 3rem;
      --space-16: 4rem;

      /* Animation */
      --ease-out: cubic-bezier(0.4, 0, 0.2, 1);
      --duration-fast: 150ms;
      --duration-normal: 250ms;
      --duration-slow: 400ms;
    }
    ```

    In app/layout.tsx:
    1. Import Inter from next/font/google alongside existing fonts
    2. Add inter.variable to the body className
    3. Update Tailwind config to reference the Inter variable

    ```tsx
    import { Cormorant_Garamond, EB_Garamond, Inter } from 'next/font/google'

    const inter = Inter({
      subsets: ['latin'],
      variable: '--font-inter',
      display: 'swap',
    })

    // In body className:
    className={`${cormorantGaramond.variable} ${ebGaramond.variable} ${inter.variable} ...`}
    ```

    Update tailwind.config.js fontFamily to include inter:
    ```js
    fontFamily: {
      cormorant: ['var(--font-cormorant)', 'serif'],
      'eb-garamond': ['var(--font-eb-garamond)', 'serif'],
      inter: ['var(--font-inter)', 'sans-serif'],  // ADD THIS
    },
    ```
  </action>
  <verify>
    Check: grep -c "color-sumi" app/globals.css (should be >= 1)
    Check: grep "Inter" app/layout.tsx (should find Inter import)
    Check: grep "font-inter" tailwind.config.js (should find inter family)
  </verify>
  <done>
    CSS custom properties defined for all design tokens, Inter font imported and configured in Tailwind
  </done>
</task>

<task type="auto">
  <name>Task 3: Update animation variants in app/page.tsx</name>
  <files>app/page.tsx</files>
  <action>
    Update the animation timing per DESIGN-SYSTEM.md specifications:

    1. Reduce staggerChildren from 0.15 (150ms) to 0.1 (100ms)
    2. Reduce item transition duration from 0.4 to 0.3

    Current (lines 16-29):
    ```tsx
    const containerVariants = {
      hidden: { opacity: 0 },
      visible: {
        opacity: 1,
        transition: {
          staggerChildren: 0.15,  // CHANGE TO 0.1
          delayChildren: 0.2
        }
      }
    }

    const itemVariants = {
      hidden: { opacity: 0, x: -20 },
      visible: { opacity: 1, x: 0, transition: { duration: 0.4 } }  // CHANGE TO 0.3
    }
    ```

    Updated:
    ```tsx
    const containerVariants = {
      hidden: { opacity: 0 },
      visible: {
        opacity: 1,
        transition: {
          staggerChildren: 0.1,   // 100ms between items (faster, per DESIGN-SYSTEM.md)
          delayChildren: 0.2
        }
      }
    }

    const itemVariants = {
      hidden: { opacity: 0, x: -20 },
      visible: {
        opacity: 1,
        x: 0,
        transition: {
          duration: 0.3,  // 300ms (faster, per DESIGN-SYSTEM.md)
          ease: [0.4, 0, 0.2, 1]  // Material Design easing
        }
      }
    }
    ```
  </action>
  <verify>
    Check: grep "staggerChildren: 0.1" app/page.tsx (should find)
    Check: grep "duration: 0.3" app/page.tsx (should find)
  </verify>
  <done>
    Animation timing reduced to 100ms stagger and 300ms duration per DESIGN-SYSTEM.md
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 6 Japanese color tokens exist in tailwind.config.js
2. CSS custom properties defined in globals.css
3. Inter font imported and configured
4. Animation timing updated to 100ms stagger, 300ms duration
5. No TypeScript errors (existing code still works with brand-* colors)
</verification>

<success_criteria>
- tailwind.config.js contains sumi, washi, beni, kincha, hai, kitsune color tokens
- app/globals.css has --color-* custom properties
- app/layout.tsx imports Inter font
- app/page.tsx uses 0.1 staggerChildren and 0.3 duration
- Existing presentation still renders (brand-* colors preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/05-responsive-performance/05-01-SUMMARY.md`
</output>
