---
phase: 03-content-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/slides/content.ts
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "User sees Three Systems as interconnected strategy (not isolated tactics)"
    - "User understands each system's purpose from one-liner overview"
    - "User can access detailed explanation of Social Media Engine"
    - "Content remains scannable with headlines and bullets"
  artifacts:
    - path: "lib/slides/content.ts"
      provides: "Three Systems overview + Social Media deep dive slides"
      contains: "Social Media Engine"
    - path: "app/page.tsx"
      provides: "Renders 2-3 new slides (overview + deep dive)"
      contains: "slide.layout"
  key_links:
    - from: "Three Systems overview slide"
      to: "Social Media deep dive slide"
      via: "section metadata and breadcrumb navigation"
      pattern: "section.*systems"
---

<objective>
Introduce the Three Systems framework as interconnected strategy and provide detailed Social Media Engine deep dive.

Purpose: Transition from problem (Empty Hours) to solution (Three Systems) with clear flywheel relationship, then demonstrate depth with first system details.

Output: Three Systems overview slide + Social Media Engine deep dive slide(s) with problem/solution framing.
</objective>

<execution_context>
@C:\Users\jlum3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jlum3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\github\sushi-media\.planning\PROJECT.md
@C:\github\sushi-media\.planning\ROADMAP.md
@C:\github\sushi-media\.planning\STATE.md
@C:\github\sushi-media\.planning\phases\03-content-architecture\03-CONTEXT.md

# Prior plan output
@C:\github\sushi-media\lib\slides\content.ts
@C:\github\sushi-media\app\page.tsx
</context>

<tasks>

<task type="auto">
  <name>Add Three Systems overview slide</name>
  <files>lib/slides/content.ts</files>
  <action>
Add Three Systems overview slide to `slides` array in `lib/slides/content.ts`.

Slide structure (id: 2):
```typescript
{
  id: 2,
  section: 'systems',
  title: 'Three Interconnected Systems',
  subtitle: 'Each system reinforces the others — a revenue flywheel',
  bullets: [
    {
      main: 'Social Media Engine',
      sub: ['Build daily presence, create buzz, fill empty seats']
    },
    {
      main: 'Corporate Lunch Machine',
      sub: ['Transform dead lunchtimes into recurring revenue']
    },
    {
      main: 'Weekday Bar Activation',
      sub: ['Turn slow evenings into social destination']
    }
  ],
  background: 'bg-brand-red',
  layout: 'content'
}
```

Follow 03-CONTEXT.md decisions:
- Show all 3 systems with names + one-liner each
- Use sub-bullets (indented, smaller text) for the one-liners
- Emphasize flywheel/reinforcing relationship in subtitle
- Confident tone: "We know what works"

Update `sections` array to reflect new slide:
```typescript
{ id: 'systems', name: 'Three Systems', slides: [2, 3, 4, 5] }
```
</action>
  <verify>
- TypeScript compiles with no errors
- Slide 3 (id: 2) exists with title "Three Interconnected Systems"
- Three bullets with sub-bullets for system names + one-liners
- Background is brand red (`bg-brand-red`)
  </verify>
  <done>Three Systems overview slide added to content structure with all three system names and one-liners</done>
</task>

<task type="auto">
  <name>Add Social Media Engine deep dive slide</name>
  <files>lib/slides/content.ts</files>
  <action>
Add Social Media Engine deep dive slide to `slides` array (id: 3).

Follow 03-CONTEXT.md deep dive format:
- Emphasize: Problem + System solution (gap this system fills and how)
- Platform specificity: Category only ("Social platforms" not specific names)
- Flywheel connection: Implied, focus on the system itself
- Visual structure: Similar to other content slides

Slide structure:
```typescript
{
  id: 3,
  section: 'systems',
  title: 'Social Media Engine',
  subtitle: 'Daily presence drives discovery and fills empty seats',
  bullets: [
    'Problem: Hidden gem with no voice in noisy market',
    'Solution: Consistent daily content on social platforms',
    {
      main: 'Showcase premium quality and social atmosphere',
      sub: ['Behind-the-scenes content', 'Event highlights', 'Community stories']
    },
    'Drive lunchtime traffic and weekday evening bookings',
    'Build loyalty beyond transactions'
  ],
  background: 'bg-gradient-to-br from-gray-900 to-black',
  layout: 'content'
}
```

Claude's discretion on exact bullet count (4-6 main bullets or 3-4 with sub-bullets). Use sub-bullets if helpful for hierarchy, but keep each bullet punchy (5-10 words).

Tone: Confident and direct. Show the gap (problem) and how this system fills it.
</action>
  <verify>
- TypeScript compiles with no errors
- Slide 4 (id: 3) exists with title "Social Media Engine"
- Content shows problem/solution framing
- Bullets are scannable (5-10 words each)
- Platform specificity is category-level (not specific app names)
  </verify>
  <done>Social Media Engine deep dive slide added with problem/solution framing and actionable details</done>
</task>

<task type="auto">
  <name>Update presentation page for new slide count</name>
  <files>app/page.tsx</files>
  <action>
Verify `app/page.tsx` correctly uses `slides.length` for totalSlides (already implemented in Plan 03-01).

If slide rendering needs enhancement for sub-bullets, update mapping logic:

```tsx
{slide.bullets.map((bullet, i) => (
  <li key={i} className="flex items-start">
    <span className="text-brand-red mr-3 mt-1">•</span>
    {typeof bullet === 'string' ? (
      <span>{bullet}</span>
    ) : (
      <div>
        <div className="font-semibold">{bullet.main}</div>
        {bullet.sub && (
          <ul className="ml-6 mt-2 space-y-1 text-base md:text-lg text-gray-300">
            {bullet.sub.map((subItem, j) => (
              <li key={j}>– {subItem}</li>
            ))}
          </ul>
        )}
      </div>
    )}
  </li>
))}
```

This handles both simple string bullets and complex bullets with sub-items.

If rendering already supports sub-bullets from Plan 03-01, verify it works correctly.
</action>
  <verify>
- `npm run dev` starts without errors
- Navigate to slide 3 (Three Systems overview) — verify 3 main bullets with sub-bullets render
- Navigate to slide 4 (Social Media Engine) — verify bullets and sub-bullets display correctly
- Slide counter shows "3 / 4" and "4 / 4"
- All navigation methods work
  </verify>
  <done>Presentation page renders 4 slides total with proper sub-bullet support</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Three Systems overview slide introducing the strategic framework, plus Social Media Engine deep dive with problem/solution framing.
  </what-built>
  <how-to-verify>
1. Visit http://localhost:3000 and navigate to slide 3 (Three Systems)
2. Verify slide shows headline "Three Interconnected Systems" with subtitle about flywheel
3. Confirm all 3 system names appear with one-liner explanations (as sub-bullets)
4. Navigate to slide 4 (Social Media Engine deep dive)
5. Verify problem/solution framing is clear
6. Check that content is scannable (headlines + bullets, not paragraphs)
7. Confirm sub-bullets render with proper indentation and smaller text
8. Test slide counter shows "3 / 4" and "4 / 4"
  </how-to-verify>
  <resume-signal>Type "approved" to continue or describe content/layout adjustments needed</resume-signal>
</task>

</tasks>

<verification>
1. Three Systems overview slide exists with all system names
2. Social Media Engine deep dive exists with problem/solution structure
3. Sub-bullets render correctly with visual hierarchy
4. Navigation works with updated slide count (4 total)
5. Content follows 03-CONTEXT.md decisions (scannable, confident tone)
6. Human verifies strategic framing is clear
</verification>

<success_criteria>
- [ ] Slide 3 (Three Systems overview) added with 3 system names + one-liners
- [ ] Slide 4 (Social Media Engine deep dive) added with problem/solution bullets
- [ ] Sub-bullets render with indentation and smaller text
- [ ] Slide counter shows "3 / 4" and "4 / 4"
- [ ] TypeScript compiles with no errors
- [ ] Human approves strategic narrative flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-architecture/03-02-SUMMARY.md`
</output>
