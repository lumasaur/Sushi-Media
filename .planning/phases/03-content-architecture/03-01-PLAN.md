---
phase: 03-content-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - lib/slides/content.ts
autonomous: true

must_haves:
  truths:
    - "User sees clean title slide with single message 'Premium Sushi with Social Soul'"
    - "User immediately encounters 'Capturing the Empty Hours' hook on slide 2"
    - "Slide content is scannable with headlines and bullets (no paragraph blocks)"
  artifacts:
    - path: "lib/slides/content.ts"
      provides: "Centralized slide content data structure"
      exports: ["slides", "sections", "SlideData type"]
    - path: "app/page.tsx"
      provides: "Presentation page rendering slide content"
      min_lines: 100
      contains: "slides.map"
  key_links:
    - from: "app/page.tsx"
      to: "lib/slides/content.ts"
      via: "import { slides, sections }"
      pattern: "import.*slides.*from.*lib/slides"
    - from: "app/page.tsx"
      to: "useSlideNavigation hook"
      via: "totalSlides prop from slides.length"
      pattern: "totalSlides.*slides\\.length"
---

<objective>
Establish slide content architecture and create opening slides that hook stakeholders with the core value proposition.

Purpose: Foundation for all content slides with consistent data structure, plus the critical first impression (title + opportunity framing) that determines whether stakeholders stay engaged.

Output: Centralized slide data structure and first 2 slides (Title + Empty Hours) rendered in presentation.
</objective>

<execution_context>
@C:\Users\jlum3\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jlum3\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\github\sushi-media\.planning\PROJECT.md
@C:\github\sushi-media\.planning\ROADMAP.md
@C:\github\sushi-media\.planning\STATE.md
@C:\github\sushi-media\.planning\phases\03-content-architecture\03-CONTEXT.md

# Prior phase outputs
@C:\github\sushi-media\app\page.tsx
@C:\github\sushi-media\components\presentation\SlideContainer.tsx
@C:\github\sushi-media\components\presentation\Breadcrumbs.tsx
</context>

<tasks>

<task type="auto">
  <name>Create slide content data structure</name>
  <files>lib/slides/content.ts</files>
  <action>
Create `lib/slides/content.ts` with TypeScript types and data structure for all presentation slides.

Structure:
```typescript
export interface SlideData {
  id: number
  section: string
  title: string
  subtitle?: string
  bullets: Array<string | { main: string; sub: string[] }>
  background?: string  // Tailwind class like 'bg-brand-black'
  layout?: 'title' | 'content' | 'cta'  // Hint for rendering pattern
}

export const sections = [
  { id: 'intro', name: 'Introduction', slides: [0, 1] },
  { id: 'systems', name: 'Three Systems', slides: [2, 3, 4, 5] },
  { id: 'execution', name: 'Execution', slides: [6, 7] }
]

export const slides: SlideData[] = [
  // Slide 0: Title
  {
    id: 0,
    section: 'intro',
    title: 'Premium Sushi with Social Soul',
    bullets: [],
    background: 'bg-brand-black',
    layout: 'title'
  },

  // Slide 1: Empty Hours Hook
  {
    id: 1,
    section: 'intro',
    title: 'Capturing the Empty Hours',
    subtitle: 'Untapped revenue in lunch and weekday evenings',
    bullets: [
      'Weekday lunchtimes sit empty',
      'Evening hours slow Monday-Thursday',
      'Premium kitchen ready, audience missing',
      'Three systems turn downtime into revenue'
    ],
    background: 'bg-gradient-to-br from-gray-900 to-black',
    layout: 'content'
  }

  // Additional slides added in subsequent plans
]
```

Follow 03-CONTEXT.md decisions:
- Bullets: 4-6 main bullets per slide (or 3-4 with sub-bullets)
- Bullet length: 5-10 words, punchy, action-oriented
- Title slide: Clean single message only
- Empty Hours slide: Hook without supporting context
</action>
  <verify>
- `npm run type-check` passes with no errors in lib/slides/content.ts
- File exports `slides` array, `sections` array, and `SlideData` type
- Title slide (id: 0) has title only, no bullets
- Empty Hours slide (id: 1) has 4 bullets matching opportunity framing
  </verify>
  <done>Slide content data structure exists with TypeScript types, sections metadata, and first 2 slides (Title + Empty Hours) defined</done>
</task>

<task type="auto">
  <name>Integrate slide content into presentation page</name>
  <files>app/page.tsx</files>
  <action>
Update `app/page.tsx` to render slides from centralized content structure instead of hardcoded placeholders.

Changes:
1. Import slide data: `import { slides, sections } from '@/lib/slides/content'`
2. Update `totalSlides` to use actual count: `const totalSlides = slides.length`
3. Replace 3 placeholder `<SlideContainer>` blocks with dynamic rendering:

```tsx
{slides.map((slide) => (
  <SlideContainer
    key={slide.id}
    slideNumber={slide.id}
    isActive={currentSlide === slide.id}
    background={slide.background || 'bg-white'}
  >
    <div className="h-full flex items-center justify-center px-12 md:px-24">
      <div className="max-w-4xl w-full">
        {/* Title slide layout */}
        {slide.layout === 'title' && (
          <div className="text-center text-white">
            <h1 className="text-5xl md:text-6xl font-bold">{slide.title}</h1>
          </div>
        )}

        {/* Content slide layout */}
        {slide.layout === 'content' && (
          <div className="text-white">
            <h2 className="text-4xl md:text-5xl font-bold mb-4">{slide.title}</h2>
            {slide.subtitle && (
              <p className="text-xl md:text-2xl text-gray-300 mb-8">{slide.subtitle}</p>
            )}
            <ul className="space-y-4 text-lg md:text-xl">
              {slide.bullets.map((bullet, i) => (
                <li key={i} className="flex items-start">
                  <span className="text-brand-red mr-3 mt-1">•</span>
                  <span>{bullet}</span>
                </li>
              ))}
            </ul>
          </div>
        )}
      </div>
    </div>
  </SlideContainer>
))}
```

4. Keep existing ProgressBar, SlideCounter, NavigationHints, and Archive link unchanged
5. Remove desktop keyboard hint (redundant with NavigationHints)

Preserve:
- All navigation hooks (useSlideNavigation, useTouchGestures)
- All UI components (ProgressBar, SlideCounter, NavigationHints)
- Archive link at bottom-left
</action>
  <verify>
- `npm run dev` starts without errors
- Visit http://localhost:3000 and see Title slide render
- Navigate to slide 2 (arrow key or swipe) and see "Capturing the Empty Hours" with 4 bullets
- Verify slide counter shows "1 / 2" on title slide, "2 / 2" on Empty Hours slide
- Confirm navigation works (keyboard, touch, URL hash)
  </verify>
  <done>Presentation page renders slides from centralized data structure with Title and Empty Hours slides displaying correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Foundation slide architecture with first 2 slides: Title slide ("Premium Sushi with Social Soul") and Empty Hours opportunity framing.
  </what-built>
  <how-to-verify>
1. Visit http://localhost:3000 in browser
2. Verify slide 1 shows clean title "Premium Sushi with Social Soul" on dark background
3. Navigate to slide 2 (arrow key, swipe, or double-tap)
4. Confirm "Capturing the Empty Hours" headline with 4 punchy bullets
5. Check that bullets are scannable (short, not full sentences)
6. Verify slide counter shows "1 / 2" and "2 / 2" correctly
7. Test navigation: keyboard arrows, touch gestures, browser back/forward all work
  </how-to-verify>
  <resume-signal>Type "approved" to continue or describe any content/layout issues</resume-signal>
</task>

</tasks>

<verification>
1. Slide data structure exists at `lib/slides/content.ts` with TypeScript types
2. Two slides render in presentation (Title + Empty Hours)
3. Content follows 03-CONTEXT.md decisions (scannable bullets, action-oriented language)
4. Navigation works with updated slide count
5. Human verifies content accuracy and visual presentation
</verification>

<success_criteria>
- [ ] `lib/slides/content.ts` exists with SlideData type, sections array, slides array
- [ ] Title slide (id: 0) renders with single clean message
- [ ] Empty Hours slide (id: 1) renders with headline, subtitle, 4 bullets
- [ ] Slide counter shows "1 / 2" and "2 / 2"
- [ ] All navigation methods work (keyboard, touch, URL)
- [ ] TypeScript compiles with no errors
- [ ] Human approves content and layout
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-architecture/03-01-SUMMARY.md`
</output>
