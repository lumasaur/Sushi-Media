---
phase: 01-foundation-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/presentation/SlideContainer.tsx
  - components/presentation/ProgressBar.tsx
  - components/presentation/SlideCounter.tsx
  - components/presentation/ScrollIndicator.tsx
  - components/presentation/Breadcrumbs.tsx
  - components/presentation/Logo.tsx
  - app/globals.css
autonomous: true

must_haves:
  truths:
    - "SlideContainer component renders slides with Framer Motion animations"
    - "ProgressBar component displays navigation progress"
    - "SlideCounter component shows current/total slide position"
    - "ScrollIndicator component appears on scrollable slides"
    - "Components compile without TypeScript errors"
  artifacts:
    - path: "components/presentation/SlideContainer.tsx"
      provides: "Animated slide wrapper with scroll support"
      exports: ["SlideContainer"]
      min_lines: 30
    - path: "components/presentation/ProgressBar.tsx"
      provides: "Progress bar for slide navigation"
      exports: ["ProgressBar"]
      min_lines: 15
    - path: "components/presentation/SlideCounter.tsx"
      provides: "Slide position indicator"
      exports: ["SlideCounter"]
      min_lines: 10
    - path: "components/presentation/ScrollIndicator.tsx"
      provides: "Scroll hint for long content"
      exports: ["ScrollIndicator"]
      min_lines: 40
    - path: "components/presentation/Breadcrumbs.tsx"
      provides: "Section navigation breadcrumbs"
      exports: ["Breadcrumbs"]
      min_lines: 30
  key_links:
    - from: "components/presentation/SlideContainer.tsx"
      to: "components/presentation/ScrollIndicator.tsx"
      via: "import statement"
      pattern: "import.*ScrollIndicator"
    - from: "app/globals.css"
      to: "scrollable-slide class"
      via: "CSS custom scrollbar styles"
      pattern: "custom-scrollbar"
---

<objective>
Migrate Kangaroo presentation components to Sushi-Media codebase, enabling slide-based presentation architecture with navigation UI elements.

Purpose: Fulfill ARCH-03 (Kangaroo components migrated with TypeScript compliance) by copying and adapting proven presentation components.

Output: 6 presentation components ready for use in new presentation route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-migration/01-RESEARCH.md

Source components (Kangaroo):
- C:\github\Kangaroo\components\presentation\SlideContainer.tsx
- C:\github\Kangaroo\components\presentation\ProgressBar.tsx
- C:\github\Kangaroo\components\presentation\SlideCounter.tsx
- C:\github\Kangaroo\components\presentation\ScrollIndicator.tsx
- C:\github\Kangaroo\components\presentation\Breadcrumbs.tsx
- C:\github\Kangaroo\components\presentation\Logo.tsx

Research findings:
- Framer Motion: v11 (Kangaroo) to v12 (Sushi-Media) - backwards compatible, no changes needed
- Path aliases: Both use @/* - direct copy works
- TypeScript: Both strict mode - compatible
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create presentation components directory and copy core components</name>
  <files>
    components/presentation/SlideContainer.tsx
    components/presentation/ProgressBar.tsx
    components/presentation/SlideCounter.tsx
    components/presentation/ScrollIndicator.tsx
    components/presentation/Breadcrumbs.tsx
    components/presentation/Logo.tsx
  </files>
  <action>
    1. Create components/presentation/ directory in Sushi-Media:
       - mkdir -p components/presentation

    2. Copy 6 core presentation components from Kangaroo:
       - SlideContainer.tsx - Main slide wrapper with animations
       - ProgressBar.tsx - Top progress indicator
       - SlideCounter.tsx - Slide number display
       - ScrollIndicator.tsx - Scroll hint for long slides
       - Breadcrumbs.tsx - Section navigation
       - Logo.tsx - Brand logo component

    3. For each component, make minimal adaptations:

       a) SlideContainer.tsx:
          - Copy as-is (uses relative import for ScrollIndicator which will work)
          - No brand color changes needed (uses bg-white default)

       b) ProgressBar.tsx:
          - Update brand color from Kangaroo green (#2B5F2E) to Ami red (#b22222)
          - Change: bg-[#2B5F2E] → bg-[#b22222]

       c) SlideCounter.tsx:
          - Update brand color from Kangaroo green to Ami red
          - Change: text-[#2B5F2E] → text-[#b22222]

       d) ScrollIndicator.tsx:
          - Copy as-is (uses neutral gray colors)
          - Verify lucide-react ChevronDown import works

       e) Breadcrumbs.tsx:
          - Update brand colors from Kangaroo green to Ami red
          - Change: text-[#2B5F2E] → text-[#b22222]
          - Change: bg-[#2B5F2E] → bg-[#b22222]
          - Change: shadow-[#2B5F2E]/50 → shadow-[#b22222]/50
          - Change: group-hover:bg-[#4A8F4E] → group-hover:bg-[#c44444]
          - NOTE: The sections array will need to be updated for Sushi-Media content in a later phase

       f) Logo.tsx:
          - This component is Kangaroo-specific (shows Kangaroo logo + MOET)
          - Create a placeholder that can be replaced with Ami Sushi branding later
          - Remove Image import and Kangaroo logo
          - Display "Ami Sushi" text with brand colors
          - Keep the structure for dark/light background adaptation

    4. Ensure all imports are valid:
       - framer-motion (already in Sushi-Media package.json)
       - lucide-react (already in Sushi-Media package.json)
       - next/image (available)
       - react (available)
  </action>
  <verify>
    - Run: ls -la components/presentation/ to confirm all 6 files created
    - Run: npm run build to verify TypeScript compilation
    - Check no import errors in terminal output
  </verify>
  <done>
    All 6 presentation components copied with brand colors updated to Ami red (#b22222), Logo placeholder created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add presentation CSS to globals.css</name>
  <files>
    app/globals.css
  </files>
  <action>
    1. Read current app/globals.css to understand existing styles

    2. Add Kangaroo presentation-specific CSS at the end of the file:

       ```css
       /* Presentation Components - Slide Navigation */
       .custom-scrollbar::-webkit-scrollbar {
         width: 8px;
       }

       .custom-scrollbar::-webkit-scrollbar-track {
         background: transparent;
       }

       .custom-scrollbar::-webkit-scrollbar-thumb {
         background: rgba(178, 34, 34, 0.3);  /* Ami red at 30% opacity */
         border-radius: 4px;
       }

       .custom-scrollbar::-webkit-scrollbar-thumb:hover {
         background: rgba(178, 34, 34, 0.5);  /* Ami red at 50% opacity on hover */
       }

       /* Scrollable slide marker for touch gesture detection */
       .scrollable-slide {
         /* Marker class - no styles needed, used for JS detection */
       }
       ```

    3. These styles are used by:
       - SlideContainer.tsx (custom-scrollbar class for smooth slide scrolling)
       - Touch gesture logic (scrollable-slide class for swipe detection)
  </action>
  <verify>
    - Run: npm run dev
    - Open browser DevTools, verify CSS is loaded
    - No CSS parsing errors in console
    - Run: npm run build to verify build passes
  </verify>
  <done>
    Custom scrollbar and scrollable-slide CSS added to globals.css with Ami brand colors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create component index file for clean exports</name>
  <files>
    components/presentation/index.ts
  </files>
  <action>
    1. Create an index.ts file in components/presentation/ for clean imports:

       ```typescript
       // Presentation Components
       // Migrated from Kangaroo presentation architecture

       export { SlideContainer } from './SlideContainer'
       export { ProgressBar } from './ProgressBar'
       export { SlideCounter } from './SlideCounter'
       export { ScrollIndicator } from './ScrollIndicator'
       export { Breadcrumbs } from './Breadcrumbs'
       export { Logo } from './Logo'
       ```

    2. This enables clean imports in presentation pages:
       - import { SlideContainer, ProgressBar } from '@/components/presentation'
       - Instead of: import { SlideContainer } from '@/components/presentation/SlideContainer'
  </action>
  <verify>
    - Run: npm run build to verify exports compile correctly
    - Verify no "Cannot find module" errors
  </verify>
  <done>
    Index file created with all component exports for clean import patterns.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build` completes without TypeScript errors
2. Component files: All 6 components exist in components/presentation/
3. Brand colors: Components use Ami red (#b22222) instead of Kangaroo green (#2B5F2E)
4. CSS added: Custom scrollbar and scrollable-slide styles in globals.css
5. Exports: index.ts provides clean export interface
6. No import errors: All framer-motion, lucide-react, next/image imports resolve
</verification>

<success_criteria>
1. SlideContainer component compiles and exports correctly
2. ProgressBar, SlideCounter, Breadcrumbs use Ami brand colors
3. ScrollIndicator works with lucide-react icon
4. Logo component adapted for Ami Sushi branding (placeholder)
5. CSS custom scrollbar uses Ami red at 30% opacity
6. All components can be imported via @/components/presentation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-migration/01-02-SUMMARY.md`
</output>
