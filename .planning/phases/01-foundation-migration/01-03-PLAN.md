---
phase: 01-foundation-migration
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Project builds without TypeScript errors after all migrations"
    - "Presentation components render in a test page"
    - "Archive routes are accessible and functional"
    - "Implementation proposal route preserved at existing path"
    - "Navigation flows correctly between all routes"
  artifacts:
    - path: "app/page.tsx"
      provides: "Placeholder presentation page testing migrated components"
      min_lines: 20
  key_links:
    - from: "app/page.tsx"
      to: "components/presentation/"
      via: "import statement"
      pattern: "import.*@/components/presentation"
    - from: "app/layout.tsx"
      to: "/archive"
      via: "navigation link"
      pattern: "href=\"/archive"
---

<objective>
Verify Phase 1 migration by testing all routes, ensuring components render correctly, and confirming the project builds without errors.

Purpose: Confirm ARCH-01 (archive), ARCH-02 (implementation proposal), and ARCH-03 (components) requirements are met before moving to Phase 2 navigation implementation.

Output: Working placeholder presentation page, verified routes, clean build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-migration/01-RESEARCH.md

Dependencies from prior plans:
- 01-01: Archive routes created (/archive, /archive/strategy-review)
- 01-02: Presentation components migrated to components/presentation/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder presentation page with component test</name>
  <files>
    app/page.tsx
  </files>
  <action>
    1. Create a new app/page.tsx that serves as placeholder for presentation AND tests migrated components:

       ```tsx
       'use client'

       import React, { useState } from 'react'
       import { SlideContainer, ProgressBar, SlideCounter } from '@/components/presentation'

       export default function PresentationPage() {
         const [currentSlide, setCurrentSlide] = useState(0)
         const totalSlides = 3

         return (
           <div className="relative h-screen overflow-hidden">
             {/* Progress and counter to verify components work */}
             <ProgressBar currentSlide={currentSlide} totalSlides={totalSlides} />
             <SlideCounter currentSlide={currentSlide} totalSlides={totalSlides} />

             {/* Test slides */}
             <SlideContainer slideNumber={0} isActive={currentSlide === 0} background="bg-brand-black">
               <div className="h-full flex items-center justify-center">
                 <div className="text-center text-white">
                   <h1 className="text-4xl font-bold mb-4">Ami Sushi Presentation</h1>
                   <p className="text-xl text-gray-300">Premium Sushi with Social Soul</p>
                   <p className="mt-8 text-sm text-gray-400">
                     Components migrated successfully. Phase 2 will add navigation.
                   </p>
                 </div>
               </div>
             </SlideContainer>

             {/* Navigation for testing */}
             <div className="fixed bottom-8 right-8 z-50 flex gap-2">
               <button
                 onClick={() => setCurrentSlide(Math.max(0, currentSlide - 1))}
                 className="px-4 py-2 bg-brand-red text-white rounded-lg hover:bg-red-700"
                 disabled={currentSlide === 0}
               >
                 Prev
               </button>
               <button
                 onClick={() => setCurrentSlide(Math.min(totalSlides - 1, currentSlide + 1))}
                 className="px-4 py-2 bg-brand-red text-white rounded-lg hover:bg-red-700"
                 disabled={currentSlide === totalSlides - 1}
               >
                 Next
               </button>
             </div>

             {/* Link to archive */}
             <a
               href="/archive"
               className="fixed bottom-8 left-8 z-50 text-sm text-gray-500 hover:text-brand-red underline"
             >
               View Archive
             </a>
           </div>
         )
       }
       ```

    2. This placeholder:
       - Tests SlideContainer renders with animations
       - Tests ProgressBar updates on slide change
       - Tests SlideCounter shows position
       - Uses Ami brand colors (bg-brand-black, bg-brand-red)
       - Provides temporary navigation buttons for testing
       - Links to archive for easy verification
  </action>
  <verify>
    - Run: npm run dev
    - Navigate to http://localhost:3000/
    - Verify placeholder presentation renders
    - Click Prev/Next buttons - verify ProgressBar and SlideCounter update
    - Verify no console errors
    - Verify Framer Motion animations work (slide transitions)
  </verify>
  <done>
    Placeholder presentation page renders with working migrated components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all routes and navigation</name>
  <files>
    (No files modified - verification only)
  </files>
  <action>
    1. Test each route manually in development server:

       a) Root route (/):
          - Should display new placeholder presentation
          - ProgressBar visible at top
          - SlideCounter visible at bottom
          - Prev/Next buttons functional

       b) Archive route (/archive):
          - Should display archived 50-initiative portfolio
          - Has "archived content" banner/indicator
          - Link to /archive/strategy-review works

       c) Archive strategy review (/archive/strategy-review):
          - Should display strategy review content
          - Components render correctly
          - No broken images or missing styles

       d) Implementation proposal (/strategy-review/implementation-proposal):
          - CRITICAL: Must still work at this EXACT path (ARCH-02)
          - Should display implementation proposal content
          - Not affected by archive migration

    2. Test navigation links:
       - From / → /archive (via link in placeholder)
       - From /archive → /archive/strategy-review
       - From /archive → / (back to presentation)
       - From any page → /strategy-review/implementation-proposal (via nav)

    3. Check for console errors on each route:
       - Open browser DevTools Console
       - Note any 404s, import errors, or React warnings
  </action>
  <verify>
    - All 4 routes load without errors
    - Navigation links work correctly
    - No console errors or warnings
    - ARCH-02: /strategy-review/implementation-proposal accessible
  </verify>
  <done>
    All routes verified functional with correct navigation flow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run production build and verify success</name>
  <files>
    (No files modified - verification only)
  </files>
  <action>
    1. Stop development server

    2. Run production build:
       ```bash
       npm run build
       ```

    3. Check build output for:
       - TypeScript errors (should be 0)
       - Build warnings (note but don't block on non-critical)
       - Successful completion message

    4. Verify build includes all routes:
       - Look for "Generating static pages" output
       - Confirm /archive, /archive/strategy-review listed
       - Confirm /strategy-review/implementation-proposal listed

    5. Optionally start production server and quick-test:
       ```bash
       npm run start
       ```
       - Visit http://localhost:3000/ - should work
       - Visit http://localhost:3000/archive - should work

    6. If build fails:
       - Document specific error messages
       - Most likely issues: missing imports, TypeScript type errors
       - Fix errors before marking plan complete
  </action>
  <verify>
    - `npm run build` completes with exit code 0
    - No TypeScript errors in output
    - All routes generated in build output
  </verify>
  <done>
    Production build passes, Phase 1 migration verified complete.
  </done>
</task>

</tasks>

<verification>
1. Route verification:
   - [ ] / displays placeholder presentation with working components
   - [ ] /archive displays 50-initiative portfolio with archive indicator
   - [ ] /archive/strategy-review displays strategy review content
   - [ ] /strategy-review/implementation-proposal works (ARCH-02)

2. Component verification:
   - [ ] SlideContainer animates slide transitions
   - [ ] ProgressBar updates on slide change
   - [ ] SlideCounter shows correct position

3. Build verification:
   - [ ] `npm run build` completes without errors
   - [ ] No TypeScript compilation errors
   - [ ] All routes included in static generation

4. Navigation verification:
   - [ ] All nav links work correctly
   - [ ] No broken internal links
   - [ ] No console errors on any route
</verification>

<success_criteria>
1. Project builds without errors after migration
2. Existing 50-initiative portfolio accessible at /archive (ARCH-01)
3. Implementation proposal remains at /strategy-review/implementation-proposal (ARCH-02)
4. Kangaroo components integrated and functional (ARCH-03)
5. All routes load without errors
6. Ready for Phase 2 navigation implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-migration/01-03-SUMMARY.md`
</output>
