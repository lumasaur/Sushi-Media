---
phase: 01-foundation-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/archive/page.tsx
  - app/archive/strategy-review/page.tsx
  - app/archive/strategy-review/components/
  - app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can access archived 50-initiative portfolio at /archive route"
    - "User can access original strategy review at /archive/strategy-review"
    - "Archived content displays correctly with all components functional"
    - "Navigation includes link to archive section"
  artifacts:
    - path: "app/archive/page.tsx"
      provides: "Archive landing page with links to archived content"
      min_lines: 30
    - path: "app/archive/strategy-review/page.tsx"
      provides: "Archived strategy review page (copied from app/strategy-review/page.tsx)"
      min_lines: 100
    - path: "app/layout.tsx"
      provides: "Updated navigation with archive links"
      contains: "/archive"
  key_links:
    - from: "app/layout.tsx"
      to: "/archive"
      via: "href attribute in navigation"
      pattern: "href=\"/archive"
    - from: "app/archive/page.tsx"
      to: "/archive/strategy-review"
      via: "Link component"
      pattern: "href=\"/archive/strategy-review"
---

<objective>
Archive existing Sushi-Media content to `/archive` route structure, preserving the 50-initiative portfolio and strategy review pages while clearing the root route for the new presentation.

Purpose: Fulfill ARCH-01 (existing 50-initiative portfolio moved to /archive route) and prepare codebase for new presentation content without losing valuable existing work.

Output: Archive landing page + copied content pages accessible at `/archive` route, updated navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-migration/01-RESEARCH.md

Key files from research:
- Current homepage: app/page.tsx (50-initiative portfolio, ~49KB)
- Strategy review: app/strategy-review/page.tsx (~28KB)
- Strategy components: app/strategy-review/components/
- Layout: app/layout.tsx (contains navigation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory structure and copy content</name>
  <files>
    app/archive/page.tsx
    app/archive/strategy-review/page.tsx
    app/archive/strategy-review/components/
  </files>
  <action>
    1. Create archive directory structure:
       - mkdir -p app/archive/strategy-review/components

    2. Copy current homepage to archive (this is the 50-initiative portfolio):
       - Copy app/page.tsx to app/archive/page.tsx
       - Update any hardcoded internal links from "/" to "/archive"

    3. Copy strategy review page and components:
       - Copy app/strategy-review/page.tsx to app/archive/strategy-review/page.tsx
       - Copy all files from app/strategy-review/components/ to app/archive/strategy-review/components/
       - Update any internal links to use /archive prefix where appropriate

    4. Review copied files for any hardcoded routes that need updating:
       - Search for href="/" and update to href="/archive" where referring to archived content
       - Search for href="/strategy-review" and update to href="/archive/strategy-review"
       - Leave links to /strategy-review/implementation-proposal unchanged (ARCH-02 requirement)
  </action>
  <verify>
    - Run: ls -la app/archive/ to confirm directory structure
    - Run: ls -la app/archive/strategy-review/ to confirm page and components copied
    - Run: npm run build to confirm TypeScript compilation passes
  </verify>
  <done>
    Archive directory structure exists with copied content files, internal links updated for archive context.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create archive landing page with navigation</name>
  <files>
    app/archive/page.tsx
  </files>
  <action>
    1. Replace the copied app/archive/page.tsx with a proper archive landing page that:
       - Displays a clear "Archive" heading
       - Lists all archived content with links:
         - "50-Initiative Portfolio" linking to current archive page content (embed or link to separate route)
         - "Strategy Review" linking to /archive/strategy-review
       - Includes a "Back to Presentation" link to "/"
       - Uses existing Tailwind styling patterns from the codebase
       - Maintains the Ami Sushi brand colors (brand-black, brand-red from tailwind config)

    2. The original 50-initiative content should remain accessible. Options:
       a) Keep it directly in app/archive/page.tsx as the main archive content
       b) Move to app/archive/portfolio/page.tsx and have landing link to it

       Recommended: Option (a) - Keep 50-initiative as the main archive page content with clear heading indicating archive status.

    3. Add a banner or notice at top indicating this is archived content:
       "This content has been archived. View the current presentation →"
  </action>
  <verify>
    - Run: npm run dev and navigate to http://localhost:3000/archive
    - Verify archive page displays with proper heading and archived content notice
    - Verify original 50-initiative portfolio content renders correctly
    - Verify link to /archive/strategy-review works
  </verify>
  <done>
    Archive landing page displays archived content with clear navigation and "archived" status indicator.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update site navigation for archive access</name>
  <files>
    app/layout.tsx
  </files>
  <action>
    1. Read current app/layout.tsx to understand navigation structure

    2. Update navigation in layout.tsx:
       - Replace "Strategic Marketing Framework" link (/) with "Presentation" (/)
       - Replace "Strategy Initiatives Review" link with "Archive" (/archive)
       - Keep "Implementation Proposal" link pointing to /strategy-review/implementation-proposal (per ARCH-02)

    3. Simplify navigation for new presentation focus:
       - Primary: Home/Presentation (/)
       - Secondary: Archive (/archive) - could be in dropdown or smaller link
       - Keep: Implementation Proposal (/strategy-review/implementation-proposal)

    4. Ensure navigation is responsive and works on mobile
  </action>
  <verify>
    - Run: npm run dev
    - Navigate through site using updated navigation links
    - Confirm / route loads (currently empty, but should not error)
    - Confirm /archive route displays archived content
    - Confirm /strategy-review/implementation-proposal still works (ARCH-02)
    - Run: npm run build to verify no TypeScript errors
  </verify>
  <done>
    Site navigation updated with archive link, implementation proposal preserved at existing route.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build` completes without errors
2. Route check: `/archive` displays 50-initiative portfolio with archive indicator
3. Route check: `/archive/strategy-review` displays strategy review content
4. Route check: `/strategy-review/implementation-proposal` remains accessible (ARCH-02)
5. Navigation check: All nav links functional
6. No broken links: All internal links in archived content point to valid routes
</verification>

<success_criteria>
1. Existing 50-initiative portfolio is accessible at `/archive` route
2. Implementation proposal page remains functional at `/strategy-review/implementation-proposal`
3. Project builds without TypeScript errors
4. Navigation updated to reflect new site structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-migration/01-01-SUMMARY.md`
</output>
