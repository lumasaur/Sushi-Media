import { PrioritizationMatrix } from './components/PrioritizationMatrix';

export const metadata = {
  title: 'Strategy Review | Ami Sushi Marketing Portal',
  description: 'Review and prioritize marketing strategies across 5 core business objectives'
};

export default function StrategyReviewPage() {
  return <PrioritizationMatrix />;
}
